# 任务拆解 - 个税计算器

## 1. 后端路由与视图准备
- [ ] 在 `zapp/views.py` 中新增 `tax_calculator` 视图函数，渲染前端模板。
- [ ] 在 `zapp/urls.py` 中注册路由 `path('tax_calculator', views.tax_calculator, name='tax_calculator')`。

## 2. 前端页面与统计集成
- [ ] 创建新模板文件 `zapp/templates/zapp/tax_calculator.html`。
- [ ] 在页面 `<script>` 初始化中，加入调用 `POST /apipy/api/pythongetip/` 的 IP 记录逻辑。
- [ ] 引入 Vue 3 (CDN)，搭建页面的基础响应式状态结构。

## 3. 核心计算器算法开发 (Vue 方法)
- [ ] 实现五险一金（含12%公积金）与个税（累计预扣预缴法）算法。
- [ ] 编写生成 1-12 月度明细数组的推算逻辑。
- [ ] 增加针对“加薪金额”及“生效月份”覆盖重算的逻辑。
- [ ] 实现年终奖单独计税的税率查找及计算方法。

## 4. UI与交互组件开发
- [ ] 编写现代化风格的 CSS，包含输入表单、数据表格、数据卡片等样式。
- [ ] **第一步模块**：基础月薪输入区及首屏明细表格。
- [ ] **第二步模块**：在基础表格渲染后，显示下方“加薪预测”和“年终奖”输入模块。
- [ ] 增加总收入汇总面板，展示年度各项税收及净收入数据。

## 5. 系统首页入口集成
- [ ] 打开 `zapp/templates/zapp/homepage.html`。
- [ ] 在桌面图标区（`.desktop`）新增“个税计算器”图标及其 SVG 图标资源。
- [ ] 绑定点击事件，新开窗口或跳转至 `/apipy/tax_calculator`。

## 6. 验证与调试
- [ ] 测试核心税费计算的准确性。
- [ ] 测试前后交互流程是否顺畅。
- [ ] 确认 IP 采集接口正常工作无报错。